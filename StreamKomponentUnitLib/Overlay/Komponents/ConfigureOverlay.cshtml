
@functions{
    // so this configure overlay and components can be configure earlier they SetParametersAsync.

    async void SetParametersAsync()
    {
        // set paramets like Overlay Canvas where components be draggable.
    }
}
